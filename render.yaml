services:
- type: web
  name: Kidney-Guard-
  env: python
  buildCommand: cd Kidney-Guard-backend && pip install -r requirements.txt
  startCommand: cd Kidney-Guard-backend && python -m uvicorn api:app --host 0.0.0.0 --port 10000
  autoDeploy: true
