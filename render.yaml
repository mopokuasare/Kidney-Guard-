services:
- type: web
  name: Kidney-Guard-
  env: python
  buildCommand: cd Kidneyguard-backend && pip install -r requirements.txt
  startCommand: cd Kidneyguard-backend && python -m uvicorn ckd_api:app --host 0.0.0.0 --port 10000
  autoDeploy: true
